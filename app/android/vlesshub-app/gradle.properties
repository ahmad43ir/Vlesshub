org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
# AGP 9 compatibility: use the standalone Kotlin 2.3.20 Gradle plugin
# (+ Compose compiler plugin) with the legacy DSL — the same combination
# the RootNet android-app uses with AGP 9.0.1 on this machine.
android.builtInKotlin=false
android.newDsl=false
